import React, { ReactElement, useState, useEffect } from "react";
import { Box, Button, Center, Flex, Heading, Spacer, Text } from "native-base";

export default function EntryScreen({ navigation }: RNN.EntryScreen): ReactElement {
  return (
    <Box>
      <Text>Hey</Text>
    </Box>
  );
}
